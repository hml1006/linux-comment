# 1. 介绍

## Features

**Core Features**

* Arm®v8.2-A A64, A32, and T32指令集完整实现
* 所有异常级别(EL0 to EL3)支持aarch32和aarch64执行状态
* 直接和间接分支预测的有序流水线
* MMU
* TrustZone
* SIMD和浮点架构(可选)
* 加密扩展(可选)
* Generic Interrupt Controller interface，支持外部中断分配器, 8366使用的中断控制器为GIC-600
* Generic Timer interface，支持64位计数器，可连接外部系统计数器，外部计数器提供时钟中断并向所有core广播计数

**Cache Features**

* L2 Cache(可选)
* L1 Cache保护，ECC or parity

**Debug features**

* Reliability, Availability, and Serviceability (RAS)扩展，一套错误处理框架
* Arm®v8.2-A debug logic
* Performance Monitoring Unit (PMU)
* Embedded Trace Macrocell (ETM) ，只支持指令追踪

## 总览

cortex a55 core集群由1-8个core，DynamIQ™ Shared Unit (DSU)组成

**A55 核心组件结构图:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Cortex-A55 Cluster                               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Core 0                      Core 1         ...      Core N│   │
│  │  ┌─────────────────────┐   ┌─────────────────────┐         │   │
│  │  │ IFU (指令取指单元)   │   │ IFU                 │         │   │
│  │  │  - 分支预测          │   │                     │         │   │
│  │  │  - 指令缓存接口      │   │                     │         │   │
│  │  └─────────┬───────────┘   └──────────┬──────────┘         │   │
│  │            │                          │                     │   │
│  │  ┌─────────▼───────────┐   ┌──────────▼──────────┐         │   │
│  │  │ DPU (数据处理单元)   │   │ DPU                 │         │   │
│  │  │  - 指令解码          │   │                     │         │   │
│  │  │  - 执行单元          │   │                     │         │   │
│  │  │  - SIMD/NEON        │   │                     │         │   │
│  │  │  - 浮点运算          │   │                     │         │   │
│  │  │  - PMU              │   │                     │         │   │
│  │  └─────────┬───────────┘   └──────────┬──────────┘         │   │
│  │            │                          │                     │   │
│  │  ┌─────────▼───────────┐   ┌──────────▼──────────┐         │   │
│  │  │ DCU (数据缓存单元)   │   │ DCU                 │         │   │
│  │  │  - L1 Data Cache    │   │                     │         │   │
│  │  │  - Store Buffer     │   │                     │         │   │
│  │  │  - Bus Interface    │   │                     │         │   │
│  │  └─────────┬───────────┘   └──────────┬──────────┘         │   │
│  └────────────┼──────────────────────────┼─────────────────────┘   │
│               │                          │                          │
│               ▼                          ▼                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              DSU (DynamIQ Shared Unit)                      │   │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │   │
│  │  │ L3 Cache        │  │ SCU (Snoop Ctrl)│  │  System Bus │ │   │
│  │  │  (Shared)       │  │  - 缓存一致性   │  │  Interface  │ │   │
│  │  └─────────────────┘  └─────────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**NEON(SIMD)
Embedded Trace Macrocell (ETM)<Debug&Trace>
Embedded Logic Analyzer (ELA)<Debug&Trace>
Snoop Control Unit (SCU)<Cache一致性>**
**Instruction Fetch Unit (IFU)**
从cache或内存取指并做分支预测，把指令传给DSU
**Data Processing Unit (DPU)**
解码和执行指令，执行指令过程中和内存的数据传输通过DCU完成，DPU包含PMU， 高级SIMD和浮点，加密扩展
**Performance Monitor Unit (PMU)**
包含6个性能监视器，可被配置用于收集统计每个core和内存系统的操作，用户调试和优化
**Memory Management Unit (MMU)**
MMU通过虚地址物理地址映射和翻译表中的内存属性提供细粒度内存控制。一个表项被使用后会缓存到Translation Lookaside Buﬀer (TLB)。
TLB表项包含Address Space Identiﬁers (ASIDs)避免进程调度flush TLB，TLB表项同时包含Virtual Machine Identiﬁers (VMIDs)避免虚拟机切换flush TLB

```
L1 TLBs
分为指令TLB和数据TLB
L2 TLB
统一的TLB，类似L2 Cache，可以通过使能single-bit error检测来cache保护
```

**L1 memory system**
包含Data Cache Unit (DCU), the Store Buﬀer (STB), and the Bus Interface Unit (BIU)
**DCU**
DCU管理所有load和store操作，L1 cache通过ECC保护数据，ECC scheme是单比特错误纠正双比特错误检测。
DCU 包括一个组合的本地和Load-Exclusive 和 Store-Exclusive 指令使用的全局独占监视器。
**STB**
Store Buffer
STB持有已经DPU内离开load/store流水线并且被DPU提交的store操作。STB可以访问L1 data cache，新加入填充的行，或者写L2,L3。
STB还用于在广播到cluster其他core之前对操作排队。
**BIU**
BIU包含连接L2 Cache的接口和buffer，用于分离L1 data和STB。
**L2 memory system(可选)**
**GIC CPU interface**
GIC CPU 接口与外部分发器组件集成时，是用于支持和管理集群系统中的中断的资源。
**DynamIQ™ Shared Unit**
DSU包含L3 cache并维护集群core之间的一致性。[详情](https://developer.arm.com/documentation/100453/latest/)
**Debug and trace components**
cortex-a55包含一系列调试，测试，追踪操作：

* 6个事件计数器，由PMU提供，一个循环计数器
* 6个硬件breakpoints，4个watchpoints
* Per-core指令追踪(ETM)
* Per-core支持ELA-500(嵌入式逻辑分析仪，调试用)
* AMBA 4 APB接口支持core cluster和DebugBlock
  [更多细节](https://developer.arm.com/documentation/100453/latest/)

## A72内部结构

**Cortex-A72 核心结构图:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Cortex-A72 Core                                  │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  IFU (Instruction Fetch Unit)                                │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │   │
│  │  │ PC/BTB      │  │ Branch      │  │ L1 Instruction      │  │   │
│  │  │ (程序计数器) │  │ Predictor   │  │ Cache (48KB)        │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Decode & Issue                                              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │   │
│  │  │ Instruction │  │ Register    │  │ Issue & Schedule    │  │   │
│  │  │ Decoder     │  │ Renamer     │  │ (乱序执行调度)      │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│         ┌────────────────────┼────────────────────┐                │
│         ▼                    ▼                    ▼                │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐           │
│  │  ALU Pipe   │    │  ALU Pipe   │    │  ALU Pipe   │           │
│  │  (整数运算) │    │  (整数运算) │    │  (整数运算) │           │
│  └─────────────┘    └─────────────┘    └─────────────┘           │
│         │                    │                    │                │
│  ┌──────┴──────┐    ┌──────┴──────┐    ┌──────┴──────┐           │
│  │  Load/Store │    │  SIMD/FP    │    │  SIMD/FP    │           │
│  │  Unit       │    │  Unit       │    │  Unit       │           │
│  └─────────────┘    └─────────────┘    └─────────────┘           │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  L1 Data Cache (32KB) + L2 Cache Interface                 │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │   │
│  │  │ Store Buffer│  │ Load Buffer │  │ Bus Interface Unit  │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```


## 异常等级

**ARMv8-A 异常等级架构:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      EL3 (Secure Monitor)                          │
│  - TrustZone安全监控器                                             │
│  - 安全/非安全状态切换                                              │
│  - 最高特权级别                                                    │
│  - 处理SMC指令                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                      EL2 (Hypervisor)                              │
│  - 虚拟机管理程序                                                   │
│  - 虚拟化支持                                                       │
│  - 处理HVC指令                                                      │
│  - 控制Guest OS                                                    │
├─────────────────────────────────────────────────────────────────────┤
│                      EL1 (Kernel/OS)                               │
│  - 操作系统内核                                                     │
│  - 内存管理 (MMU)                                                  │
│  - 中断处理                                                         │
│  - 处理SVC指令                                                      │
│  - 管理系统资源                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                      EL0 (User/Application)                        │
│  - 用户应用程序                                                     │
│  - 最低特权级别                                                    │
│  - 只能访问自己的虚拟地址空间                                        │
│  - 无直接硬件访问权限                                                │
└─────────────────────────────────────────────────────────────────────┘

特权等级: EL3 > EL2 > EL1 > EL0
```

## PMU

**性能监控单元 (Performance Monitor Unit):**

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PMU Architecture                            │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  PMU (Performance Monitor Unit)                            │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  6 个可编程事件计数器 (PMEVCNTR0-PMEVCNTR5)          │   │   │
│  │  │  - 每个计数器 32/64 位                                │   │   │
│  │  │  - 可配置监控不同事件类型                              │   │   │
│  │  │  - 支持溢出中断                                       │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  1 个循环计数器 (PMCCNTR)                            │   │   │
│  │  │  - 监控 CPU 时钟周期                                  │   │   │
│  │  │  - 始终运行 (除非禁用)                                 │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  事件选择寄存器 (PMSELR, PMEVTYPER0-PMEVTYPER5)     │   │   │
│  │  │  - 配置每个计数器监控的事件                           │   │   │
│  │  │  - 支持多种事件: 指令执行、缓存命中/未命中、分支预测等  │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  控制寄存器 (PMCR, PMCNTENSET, PMCNTENCLR)          │   │   │
│  │  │  - 启用/禁用计数器                                    │   │   │
│  │  │  - 设置计数器模式                                     │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    监控事件类型                               │   │
│  │  • L1I/L1D Cache 访问/未命中                                 │   │
│  │  • L2 Cache 访问/未命中                                      │   │
│  │  • 指令执行/分支/分支预测                                    │   │
│  │  • 内存访问/存储                                             │   │
│  │  • TLB 访问/未命中                                           │   │
│  │  • SIMD/浮点操作                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## DSU

**DynamIQ Shared Unit (DSU) 结构图:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                       DSU Architecture                               │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              DynamIQ Shared Unit (DSU)                       │   │
│  │                                                             │   │
│  │  ┌───────────────┐  ┌───────────────┐                       │   │
│  │  │   Core 0      │  │   Core 1      │        ...           │   │
│  │  │  (A55/A72等) │  │  (A55/A72等) │                       │   │
│  │  └───────┬───────┘  └───────┬───────┘                       │   │
│  │          │                  │                                │   │
│  │  ┌───────▼──────────────────▼───────┐                       │   │
│  │  │        SCU (Snoop Control Unit)  │                       │   │
│  │  │  - 维护 cluster 内缓存一致性     │                       │   │
│  │  │  - 处理 cache snooping          │                       │   │
│  │  │  - 仲裁内存访问                  │                       │   │
│  │  └──────────────────┬───────────────┘                       │   │
│  │                     │                                       │   │
│  │  ┌──────────────────▼───────────────┐                       │   │
│  │  │       L3 Cache (Shared)          │                       │   │
│  │  │  - 统一指令/数据缓存              │                       │   │
│  │  │  - 支持 4-32MB 可配置            │                       │   │
│  │  │  - 低延迟访问                    │                       │   │
│  │  └──────────────────┬───────────────┘                       │   │
│  │                     │                                       │   │
│  │  ┌──────────────────▼───────────────┐                       │   │
│  │  │       System Bus Interface       │                       │   │
│  │  │  - 连接外部内存控制器            │                       │   │
│  │  │  - AMBA AXI 接口                 │                       │   │
│  │  │  - 支持多主设备                  │                       │   │
│  │  └──────────────────────────────────┘                       │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  其他组件:                                          │   │   │
│  │  │  • CCI-550 (Cache Coherent Interconnect)            │   │   │
│  │  │  • GIC (中断控制器)                                  │   │   │
│  │  │  • Generic Timer (系统计时器)                        │   │   │
│  │  │  • PMU (性能监控)                                    │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```
