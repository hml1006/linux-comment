# 8.指令集

字节（byte）：8位。
半字（halfword）：16位。
字（word）：32位。
双字（doubleword）：64位。
四字（quadword）：128位。

## 数据处理指令

数据处理指令大部分有一个目的寄存器和两个源操作数，通用格式如下：

```asm
Instruction Rd, Rn, Operand2
```

数据处理操作包括：

* 算术和逻辑操作
* 传送和移位操作
* 符号扩展和0扩展
* 位和位域处理
* 条件比较和数据处理

### 算术和逻辑操作

| 类型                                                                                    | 指令                         |
| :-------------------------------------------------------------------------------------- | :--------------------------- |
| 算术                                                                                    | ADD, SUB, ADC, SBC, NEG      |
| 逻辑                                                                                    | AND, BIC, ORR, ORN, EOR, EON |
| 比较                                                                                    | CMP, CMN, TST                |
| 传送                                                                                    | MOV, MVN                     |
| 有些指令包含 S 后缀，这些指令可能会影响到PSTSTE寄存器里面的条件标志位，比如进位、溢出。 |                              |
| 这些指令包括ADDS, SUBS, ADCS, SBCS, ANDS, and BICS.                                     |                              |
| 还有一些会影响到标志位的指令没有 S 后缀，CMP, CMN and TST。                             |                              |
| ADC, SBC 指令和ADD，SUB指令不同的地方在于会把进位标志作为输入带上， 如下：              |                              |

```asm
ADC{S}: Rd = Rn + Rm + C
SBC{S}: Rd = Rn - Rm - 1 + C
```

算术指令例子：

```
ADD W0, W1, W2, LSL #3    // W0 = W1 + (W2 << 3)
SUBS X0, X4, X3, ASR #2    // X0 = X4 - (X3 >> 2), set flags
MOV X0, X1    // Copy X1 to X0
CMP W3, W4    // Set flags based on W3 - W4
ADD W0, W5, #27    // W0 = W5 + 27
```

### 乘法除法指令

基本乘法指令 MUL， 乘法指令包含很多变种。

```asm
MUL X0, X1, X2    // X0 = X1 * X2
```

除法指令UDIV， SDIV，分别为无符号和有符号除法。
除0问题，x86下会抛除0异常，但是arm64会返回0。

### 移位操作

以下说明是专门针对位移的：

* 逻辑向左移位（LSL）。LSL 指令执行 2 的乘法。
* 逻辑向右移位（LSR）。LSR 指令以 2 的幂执行除法。
* 算术向右移（ASR）。ASR 指令执行 2 的幂的除法，保留符号位。
* 循环右移（ROR）。ROR 指令执行后左端空出来的bit用右端移走的bit填充。

### bitfield和字节操作指令

**Bitfield 指令:**

```
Bitfield 操作指令格式:
┌─────────────────────────────────────────────────────────────────────┐
│  BFM  (Bitfield Move)                                              │
│  BFI  (Bitfield Insert)                                            │
│  BFXIL (Bitfield Extract and Insert Long)                          │
│  BFXTR (Bitfield Extract)                                          │
│                                                                     │
│  BFI Rd, Rn, #lsb, #width                                          │
│  将 Rn 的低位 width 位插入到 Rd 的 [lsb+width-1:lsb] 位            │
│                                                                     │
│  BFXTR Rd, Rn, #lsb, #width                                        │
│  从 Rn 提取 [lsb+width-1:lsb] 位到 Rd 的低位                       │
│                                                                     │
│  示例:                                                               │
│  BFI X0, X1, #8, #16    // 将 X1 的低 16 位插入到 X0 的 [23:8]    │
│  BFXTR X0, X1, #4, #8    // 从 X1 提取 [11:4] 位到 X0              │
│                                                                     │
│  字节操作指令:                                                        │
│  ┌──────────┬─────────────────────────────────────────────────────┐  │
│  │ 指令     │  描述                                               │  │
│  ├──────────┼─────────────────────────────────────────────────────┤  │
│  │  REV     │  反转字节顺序                                        │  │
│  │  REV16   │  每半字内反转字节顺序                                │  │
│  │  REV32   │  每字内反转字节顺序                                  │  │
│  │  RBIT    │  反转所有位顺序                                     │  │
│  │  SXTB    │  字节符号扩展到 32 位                                │  │
│  │  SXTH    │  半字符号扩展到 32 位                                │  │
│  │  UXTB    │  字节零扩展到 32 位                                 │  │
│  │  UXTH    │  半字零扩展到 32 位                                 │  │
│  └──────────┴─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 条件指令

**条件代码 (Condition Codes):**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Condition Codes                               │
│                                                                     │
│  条件指令格式: <instruction>{<cond>}                               │
│                                                                     │
│  ┌──────────┬──────────┬─────────────────────────────────────────┐  │
│  │  条件码   │   助记符  │  描述                                │  │
│  ├──────────┼──────────┼─────────────────────────────────────────┤  │
│  │  0000    │   EQ      │  相等 (Z=1)                           │  │
│  │  0001    │   NE      │  不相等 (Z=0)                         │  │
│  │  0010    │   CS/HS   │  进位设置/无符号大于等于 (C=1)        │  │
│  │  0011    │   CC/LO   │  进位清除/无符号小于 (C=0)           │  │
│  │  0100    │   MI      │  负 (N=1)                             │  │
│  │  0101    │   PL      │  正或零 (N=0)                         │  │
│  │  0110    │   VS      │  溢出 (V=1)                           │  │
│  │  0111    │   VC      │  无溢出 (V=0)                         │  │
│  │  1000    │   HI      │  无符号大于 (C=1, Z=0)               │  │
│  │  1001    │   LS      │  无符号小于等于 (C=0, Z=1)           │  │
│  │  1010    │   GE      │  有符号大于等于 (N=V)                │  │
│  │  1011    │   LT      │  有符号小于 (N≠V)                    │  │
│  │  1100    │   GT      │  有符号大于 (Z=0, N=V)              │  │
│  │  1101    │   LE      │  有符号小于等于 (Z=1, N≠V)          │  │
│  │  1110    │   AL      │  始终执行 (默认)                     │  │
│  │  1111    │   NV      │  从不执行                           │  │
│  └──────────┴──────────┴─────────────────────────────────────────┘  │
│                                                                     │
│  示例:                                                               │
│  ADDGT X0, X1, X2    // 如果 GT 条件满足，执行加法                  │
│  SUBEQ X0, X1, X2    // 如果 EQ 条件满足，执行减法                  │
│  CMP X0, X1          // 设置条件标志位                             │
│  B.EQ label          // 如果相等，跳转到 label                     │
└─────────────────────────────────────────────────────────────────────┘
```

**影响 PSTATE 状态位的指令:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Instructions Affecting PSTATE                   │
│                                                                     │
│  设置状态位的指令:                                                   │
│  ┌──────────┬─────────────────────────────────────────────────────┐  │
│  │ 指令     │  影响的状态位                                        │  │
│  ├──────────┼─────────────────────────────────────────────────────┤  │
│  │  ADDS    │  NZCV                                               │  │
│  │  SUBS    │  NZCV                                               │  │
│  │  ADCS    │  NZCV                                               │  │
│  │  SBCS    │  NZCV                                               │  │
│  │  ANDS    │  NZCV                                               │  │
│  │  ORRS    │  NZCV                                               │  │
│  │  BICS    │  NZCV                                               │  │
│  │  CMP     │  NZCV (无 S 后缀)                                   │  │
│  │  CMN     │  NZCV (无 S 后缀)                                   │  │
│  │  TST     │  NZCV (无 S 后缀)                                   │  │
│  │  TEQ     │  NZCV (无 S 后缀)                                   │  │
│  └──────────┴─────────────────────────────────────────────────────┘  │
│                                                                     │
│  比较指令示例:                                                        │
│  CMP W0, W1          // 执行 W0 - W1，设置标志位                   │
│  CMN W0, W1          // 执行 W0 + W1，设置标志位                   │
│  TST W0, #0xFF       // 执行 W0 & #0xFF，设置标志位               │
│  TEQ W0, W1          // 执行 W0 ^ W1，设置标志位                   │
│                                                                     │
│  状态位含义:                                                         │
│  ┌──────┬─────────────────────────────────────────────────────────┐  │
│  │  N   │  Negative (结果为负)                                    │  │
│  │  Z   │  Zero (结果为零)                                       │  │
│  │  C   │  Carry (无符号溢出/进位)                               │  │
│  │  V   │  Overflow (有符号溢出)                                 │  │
│  └──────┴─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

## 内存访问指令

ARMv8 架构是一个加载/存储架构。这意味着没有数据处理指令直接对内存中的数据进行操作。数据必须首先被加载到寄存器中，进行修改，然后存储到内存中。程序必须指定一个地址，要传输的数据大小，以及一个源寄存器或目标寄存器。还有一些额外的加载和存储指令，提供了更多的选择，如非时间性的加载/存储，加载/存储排他性，以及获取/释放。
内存指令可以以非对齐方式访问普通内存。这不支持独占访问、加载获取或存储释放变体。如果不需要非对齐访问，可以将其配置为故障。
atomic load store指令必须是对齐的。

### load指令

Load 指令的一般形式如下：

```asm
LDR Rt, <addr>
```

对于加载到整数寄存器中，你可以选择一个大小来加载。例如，要加载一个比指定的寄存器值小的尺寸，在
LDR 指令中加入以下后缀之一：

* LDRB (8-bit, zero extended).
* LDRSB (8-bit, sign extended).
* LDRH (16-bit, zero extended).
* LDRSH (16-bit, sign extended).
* LDRSW (32-bit, sign extended).

**符号扩展规则:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Sign Extension Rules                          │
│                                                                     │
│  零扩展 (Zero Extend) - 高位填充 0:                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDRB  X0, [X1]  // 8-bit → 64-bit (零扩展)               │   │
│  │  LDRH  X0, [X1]  // 16-bit → 64-bit (零扩展)             │   │
│  │  UXTB  W0, W1    // 8-bit → 32-bit (零扩展)               │   │
│  │  UXTH  W0, W1    // 16-bit → 32-bit (零扩展)             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  符号扩展 (Sign Extend) - 高位填充符号位:                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDRSB X0, [X1]  // 8-bit → 64-bit (符号扩展)             │   │
│  │  LDRSH X0, [X1]  // 16-bit → 64-bit (符号扩展)           │   │
│  │  LDRSW X0, [X1]  // 32-bit → 64-bit (符号扩展)           │   │
│  │  SXTB  W0, W1    // 8-bit → 32-bit (符号扩展)             │   │
│  │  SXTH  W0, W1    // 16-bit → 32-bit (符号扩展)           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  示例:                                                               │
│  原始值: 0xFF (8-bit)                                                │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  零扩展: 0x00000000000000FF                                 │   │
│  │  符号扩展: 0xFFFFFFFFFFFFFFFF                               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  原始值: 0x7F (8-bit)                                                │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  零扩展: 0x000000000000007F                                 │   │
│  │  符号扩展: 0x000000000000007F                                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### store指令格式

store指令一般形式：

```asm
STR Rn，<addr>
```

### 访问多个内存位置

**LDP/STP 指令:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      LDP/STP Instructions                          │
│                                                                     │
│  LDP (Load Pair):                                                  │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDP Xt1, Xt2, [Xn, #offset]                               │   │
│  │  从内存加载两个连续寄存器                                   │   │
│  │                                                             │   │
│  │  示例:                                                       │   │
│  │  LDP X0, X1, [X2]         // X0 = [X2], X1 = [X2+8]       │   │
│  │  LDP W0, W1, [X2, #16]    // W0 = [X2+16], W1 = [X2+20]  │   │
│  │  LDP X0, X1, [X2, #-16]!  // X0 = [X2-16], X1 = [X2-8], │   │
│  │                            // X2 = X2 - 16              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  STP (Store Pair):                                                 │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  STP Xt1, Xt2, [Xn, #offset]                              │   │
│  │  将两个连续寄存器存储到内存                                 │   │
│  │                                                             │   │
│  │  示例:                                                       │   │
│  │  STP X0, X1, [X2]         // [X2] = X0, [X2+8] = X1       │   │
│  │  STP W0, W1, [X2, #16]    // [X2+16] = W0, [X2+20] = W1  │   │
│  │  STP X0, X1, [X2, #-16]!  // [X2-16] = X0, [X2-8] = X1, │   │
│  │                            // X2 = X2 - 16              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  栈操作常用模式:                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  STP X29, X30, [SP, #-16]!  // 保存 FP 和 LR 到栈         │   │
│  │  ...                                                  │   │
│  │  LDP X29, X30, [SP], #16    // 恢复 FP 和 LR 并调整栈    │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### Load Store例子

**Load/Store 示例:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Load/Store Examples                             │
│                                                                     │
│  基本加载:                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR X0, [X1]         // X0 = *X1 (加载8字节)              │   │
│  │  LDR W0, [X1]         // W0 = *(int*)X1 (加载4字节)       │   │
│  │  LDRB W0, [X1]        // W0 = *(char*)X1 (加载1字节)      │   │
│  │  LDRH W0, [X1]        // W0 = *(short*)X1 (加载2字节)     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  基本存储:                                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  STR X0, [X1]         // *X1 = X0 (存储8字节)              │   │
│  │  STR W0, [X1]         // *(int*)X1 = W0 (存储4字节)       │   │
│  │  STRB W0, [X1]        // *(char*)X1 = W0 (存储1字节)      │   │
│  │  STRH W0, [X1]        // *(short*)X1 = W0 (存储2字节)     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  带偏移量的加载/存储:                                                 │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR X0, [X1, #8]     // X0 = *(X1 + 8)                  │   │
│  │  STR X0, [X1, #-8]    // *(X1 - 8) = X0                 │   │
│  │  LDR X0, [X1, X2, LSL #3]  // X0 = *(X1 + X2 * 8)      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  后索引/前索引:                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR X0, [X1], #8     // X0 = *X1, X1 += 8              │   │
│  │  STR X0, [X1], #8     // *X1 = X0, X1 += 8              │   │
│  │  LDR X0, [X1, #8]!    // X1 += 8, X0 = *X1             │   │
│  │  STR X0, [X1, #8]!    // X1 += 8, *X1 = X0             │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 符号扩展和0扩展

**扩展指令:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Extension Instructions                          │
│                                                                     │
│  零扩展 (Zero Extension):                                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  UXTB  Wd, Wn     // Wn[7:0] → Wd (高位填0)              │   │
│  │  UXTH  Wd, Wn     // Wn[15:0] → Wd (高位填0)             │   │
│  │  UXTW  Xd, Wn     // Wn[31:0] → Xd (高位填0)             │   │
│  │  UXTB16 Wd, Wn    // 每16位零扩展                       │   │
│  │  UXTH16 Wd, Wn    // 每16位零扩展                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  符号扩展 (Sign Extension):                                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  SXTB  Wd, Wn     // Wn[7:0]符号扩展 → Wd               │   │
│  │  SXTH  Wd, Wn     // Wn[15:0]符号扩展 → Wd              │   │
│  │  SXTW  Xd, Wn     // Wn[31:0]符号扩展 → Xd              │   │
│  │  SXTB16 Wd, Wn    // 每16位符号扩展                      │   │
│  │  SXTH16 Wd, Wn    // 每16位符号扩展                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  示例:                                                               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  MOV W1, #0xFF                                                │   │
│  │  UXTB W0, W1      // W0 = 0x000000FF                       │   │
│  │  SXTB W0, W1      // W0 = 0xFFFFFFFF                       │   │
│  │                                                             │   │
│  │  MOV W1, #0x7F                                                │   │
│  │  UXTB W0, W1      // W0 = 0x0000007F                       │   │
│  │  SXTB W0, W1      // W0 = 0x0000007F                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 寻址方式

**寻址模式 (Addressing Modes):**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Addressing Modes                              │
│                                                                     │
│  无偏移量寻址:                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR Xt, [Xn]         // 地址 = Xn                        │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  立即数偏移量寻址:                                                   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR Xt, [Xn, #imm]    // 地址 = Xn + imm                │   │
│  │  LDR Xt, [Xn, #-imm]   // 地址 = Xn - imm                │   │
│  │  imm 范围: 0~255 (乘以元素大小)                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  寄存器偏移量寻址:                                                   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR Xt, [Xn, Xm]      // 地址 = Xn + Xm                │   │
│  │  LDR Xt, [Xn, Xm, LSL #n]  // 地址 = Xn + (Xm << n)    │   │
│  │  LDR Xt, [Xn, Xm, LSR #n]  // 地址 = Xn + (Xm >> n)    │   │
│  │  LDR Xt, [Xn, Xm, ASR #n]  // 地址 = Xn + (Xm >> n, 符号扩展)││
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  前索引寻址 (Pre-indexed):                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR Xt, [Xn, #imm]!   // Xn += imm, 然后访问            │   │
│  │  STR Xt, [Xn, #imm]!   // Xn += imm, 然后存储            │   │
│  │  (! 表示更新基址寄存器)                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  后索引寻址 (Post-indexed):                                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  LDR Xt, [Xn], #imm    // 先访问, 然后 Xn += imm        │   │
│  │  STR Xt, [Xn], #imm    // 先存储, 然后 Xn += imm        │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## 流程控制

A64 指令集提供了许多不同种类的分支指令。对于简单的相对分支，即那些从当前地址开始的偏移量，使用 B 指令。无条件的简单相对分支可以从当的程序计数器位置向后或向前分支，最多可达 128MB。有条件的简单相对分支，即在 B 指令上附加了一个条件代码，其范围较小，为 ±1MB。
对子程序的调用，如果需要将返回地址保存在链接寄存器（X30）中，则使用 BL 指令。这条指令没有条件版本。BL 的行为和 B 指令一样，有一个额外的效果，就是将返回地址，也就是 BL 后的指令地址，存储在寄存器 X30 中。

**分支指令 (Branch Instructions):**

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Branch Instructions                            │
│                                                                     │
│  B (Branch):                                                        │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  B label           // 无条件跳转到 label                  │   │
│  │  B.cond label      // 条件跳转                          │   │
│  │                                                             │   │
│  │  无条件分支范围: ±128MB (26-bit 偏移量 × 4)             │   │
│  │  条件分支范围: ±1MB (19-bit 偏移量 × 4)                │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  BL (Branch with Link):                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  BL label          // 跳转到 label，保存返回地址到 X30    │   │
│  │  BLX label         // 跳转到 label，切换指令集(ARM/Thumb)│   │
│  │                                                             │   │
│  │  分支范围: ±128MB                                        │   │
│  │  返回地址 = 当前 PC + 4 (下一条指令地址)                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  BR (Branch Register):                                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  BR Xn             // 跳转到 Xn 指向的地址                │   │
│  │  BLR Xn            // 跳转到 Xn，保存返回地址到 X30      │   │
│  │  RET               // 跳转到 X30 (函数返回)              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  分支指令示例:                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  B loop_start      // 无条件跳转                         │   │
│  │  B.EQ exit         // 如果相等，跳转到 exit              │   │
│  │  BL func_call      // 调用函数，返回地址保存在 X30      │   │
│  │  BLR X0            // 跳转到 X0 指向的地址，保存返回地址│   │
│  │  RET              // 从函数返回 (BR X30)               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  函数调用流程:                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  main()                                                   │   │
│  │    │                                                     │   │
│  │    ├── BL sub_func    // X30 = 返回地址 (main+4)       │   │
│  │    │                     │                             │   │
│  │    │                     ▼                             │   │
│  │    │  sub_func()                                        │   │
│  │    │    │                                             │   │
│  │    │    ├── 执行函数体                                 │   │
│  │    │    │                                             │   │
│  │    │    └── RET        // 跳转到 X30 (main+4)        │   │
│  │    │                     │                             │   │
│  │    ▼                     ▼                             │   │
│  │  main() 继续执行                                          │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## 系统控制和其他指令

### 异常处理指令

有三条异常处理指令，其目的是导致异常的发生。这些指令用于调用运行在操作系统（EL1）、管理程序（EL2）或安全监控器（EL3）中更高的异常级别的代码：

* SVC #imm16 //主管调用，允许应用程序调用内核//（EL1）。
* HVC #imm16 //虚拟机管理程序调用，允许操作系统代码调用虚拟机管理程序（EL2）。
* SMC #imm16 //安全监视器调用，允许操作系统或虚拟机管理程序调用安全//监视器（EL3）。

### 系统寄存器访问

系统寄存器访问提供了两项说明：

* MRS Xt,
  例如：MRS X4, ELR_EL1 // Copies ELR_EL1 to X4
* MSR , Xt
  例如：MSR SPSR_EL1, X0 // Copies X0 to SPSR_EL1
  PSTATE 的个别字段也可以用 MSR 或 MRS 访问。例如，要选择与 EL0 相关的堆栈指针或当前的异常级别。
* MSR SPSel, #imm // A value of 0 or 1 in this register is used to select // between using EL0 stack pointer or the current exception
  // level stack pointer
  这些指令有特殊形式可用于清除或设置单个异常掩码位（见第 4-5 页的保存进程状态寄存器）：
* MSR DAIFClr, #imm4
* MSR DAIFSet, #imm4

### 调试指令

有两个与调试相关的说明：

* BRK #imm16 // 进入监视器模式调试，其中有片上调试监视器代码
* HLT #imm16 // 进入停止模式调试，其中连接外部调试硬件
