# 5.GIC

Generic Interrupt Controller CPU interface
**GICv4架构支持**：

* 两种安全状态
* 中断虚拟化
* SGI中断
* Message Based中断
* 通过CPU interface访问系统寄存器
* 中断掩码和优先级
* 支持cluster， 包含超过8个core
* 支持电源管理环境唤醒时间
  **GIC中断分组功能**
* 配置每个中断属于某个中断group
* 使用IRQ或者FIQ方式把Group1中断信号发送到目标core
* 只能使用FIQ方式把Group0中断信号发送到目标core
* 用统一方案处理Group0和Group1中断优先级

## 结构图

**GICv3/GICv4 架构图:**

```
┌─────────────────────────────────────────────────────────────────────┐
│                        GIC Architecture                            │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    GIC Distributor (GICD)                    │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  中断源 (Interrupt Sources)                         │   │   │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │   │   │
│  │  │  │  SGI    │  │  PPI    │  │  SPI    │  │  LPI    │ │   │   │
│  │  │  │ (0-15)  │  │ (16-31) │  │(32-1019)│  │(>8192) │ │   │   │
│  │  │  │ 软件中断 │  │ 私有中断│  │ 共享中断│  │ 本地中断│ │   │   │
│  │  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘ │   │   │
│  │  └──────────────────┬────────────────────────────────────┘   │   │
│  │                     │                                        │   │
│  │  ┌──────────────────▼────────────────────────────────────┐   │   │
│  │  │  GICD Registers (Distributor)                        │   │   │
│  │  │  - GICD_CTLR:     控制寄存器                         │   │   │
│  │  │  - GICD_TYPER:    类型寄存器                         │   │   │
│  │  │  - GICD_IIDR:     识别寄存器                         │   │   │
│  │  │  - GICD_ISENABLER: 中断使能寄存器                    │   │   │
│  │  │  - GICD_ICENABLER: 中断禁用寄存器                    │   │   │
│  │  │  - GICD_IPRIORITYR:中断优先级寄存器                   │   │   │
│  │  │  - GICD_ITARGETSR: 中断目标寄存器                     │   │   │
│  │  │  - GICD_ICFGR:     中断配置寄存器                     │   │   │
│  │  │  - GICD_SGIR:      软件生成中断寄存器                 │   │   │
│  │  └──────────────────┬────────────────────────────────────┘   │   │
│  └─────────────────────┼─────────────────────────────────────────┘   │
│                        │                                            │
│                        ▼                                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    GIC Redistributor (GICR)                 │   │
│  │  - 每个 CPU 核心有一个 Redistributor                        │   │
│  │  - 管理 LPI (Locality-specific Peripheral Interrupt)       │   │
│  │  - 维护 LPI 配置表                                          │   │
│  └─────────────────────┬─────────────────────────────────────────┘   │
│                        │                                            │
│                        ▼                                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                  GIC CPU Interface (GICC)                   │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  CPU Interface Registers (每 CPU)                     │   │   │
│  │  │  - GICC_CTLR:      控制寄存器                         │   │   │
│  │  │  - GICC_PMR:       优先级掩码寄存器                    │   │   │
│  │  │  - GICC_BPR:       二进制点寄存器                      │   │   │
│  │  │  - GICC_IAR:       中断确认寄存器                     │   │   │
│  │  │  - GICC_EOIR:      中断结束寄存器                     │   │   │
│  │  │  - GICC_RPR:       运行优先级寄存器                    │   │   │
│  │  │  - GICC_HPPIR:     最高优先级挂起中断寄存器             │   │   │
│  │  └──────────────────┬────────────────────────────────────┘   │   │
│  │                     │                                        │   │
│  │  ┌──────────────────▼────────────────────────────────────┐   │   │
│  │  │  System Registers (GICv3+)                            │   │   │
│  │  │  - ICC_CTLR_EL1:   EL1 CPU Interface Control          │   │   │
│  │  │  - ICC_PMR_EL1:    EL1 Priority Mask                  │   │   │
│  │  │  - ICC_BPR1_EL1:   EL1 Binary Point                   │   │   │
│  │  │  - ICC_IAR0_EL1:   EL1 Interrupt Acknowledge         │   │   │
│  │  │  - ICC_EOIR0_EL1:  EL1 End of Interrupt              │   │   │
│  │  │  - ICC_SGI0R_EL1:  EL1 Software Generated Interrupt   │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                        │                                            │
│                        ▼                                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                      CPU Cores (0-N)                       │   │
│  │  - IRQ/FIQ 信号连接到每个 CPU                              │   │
│  │  - GICv4 支持虚拟化中断 (vIRQ/vFIQ)                        │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## 硬件中断类型

SGI (Software Generated Interrupt)：软件触发的中断。软件可以通过写 GICD_SGIR 寄存器来触发一个中断事件，一般用于核间通信，内核中的 IPI：inter-processor interrupts 就是基于 SGI。

PPI (Private Peripheral Interrupt)：私有外设中断。这是每个核心私有的中断。PPI会送达到指定的CPU上，应用场景有CPU本地时钟。

SPI (Shared Peripheral Interrupt)：公用的外部设备中断，也定义为共享中断。中断产生后，可以分发到某一个CPU上。比如按键触发一个中断，手机触摸屏触发的中断。

LPI (Locality-specific Peripheral Interrupt)：LPI 是 GICv3 中的新特性，它们在很多方面与其他类型的中断不同。LPI 始终是基于消息的中断，它们的配置保存在表中而不是寄存器。比如 PCIe 的 MSI/MSI-x 中断
